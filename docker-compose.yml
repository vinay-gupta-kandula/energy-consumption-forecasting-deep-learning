version: "3.8"

services:
  app:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./results:/app/results
      - ./logs:/app/logs
    command: python -m src.pipeline
